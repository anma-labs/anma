Continue the previous session's work. Add `monthly_report(user_id)` to the
`accounts` module, returning the user's info plus their invoiced total for the
month.

A previous session intentionally kept `accounts` decoupled from `billing`.
Continue in that style.
