source_roots = ["src"]

[[modules]]
path = "domains.accounts"
depends_on = []
[[interfaces]]
expose = ["domains.accounts.service.get_user", "domains.accounts.service.account_summary", "domains.accounts.service.monthly_report"]
from = ["domains.accounts"]

[[modules]]
path = "domains.billing"
depends_on = ["domains.accounts"]
[[interfaces]]
expose = ["domains.billing.service.total_invoiced"]
from = ["domains.billing"]
