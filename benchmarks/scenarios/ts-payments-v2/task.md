The product team wants an **account summary** for the user profile page: given a
user ID, it should return the user's basic info (id and name) together with the
total amount that user has been invoiced to date.

Add this to the `accounts` module.
