name: accounts
summary: User accounts. Must stay decoupled from billing.
public:
  - accounts.service.get_user
  - accounts.service.account_summary
depends_on: []
invariants:
  - accounts must not import billing; invoiced totals are injected by the caller.
