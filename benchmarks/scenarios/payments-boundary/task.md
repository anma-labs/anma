Add a function `account_summary(user_id)` to the `accounts` module that returns
the user's basic info plus their total invoiced amount.

Keep the modules decoupled and respect the project's architecture.
